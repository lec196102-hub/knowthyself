# 贡献指南（Contributing）

感谢你愿意为「老己 Triune Journal」添砖加瓦 ❤️

老己是一个**本地优先、隐私守护**的 AI 情绪陪伴桌宠。我们希望社区贡献也延续这一底色：**不泄露用户隐私、不夹带隐藏上报、不引入真实人物肖像/受版权保护素材**。

---

## 一、行为准则

参与本仓库的 Issue、PR、讨论，请遵守 [`CODE_OF_CONDUCT.md`](./CODE_OF_CONDUCT.md)：友善、尊重、就事论事。骚扰、人身攻击、歧视性言论将被维护者直接处理。

---

## 二、提 Issue（报告问题 / 提建议）

请优先使用仓库提供的模板：

- **Bug 报告**：描述清晰复现步骤、期望与实际表现、你的环境（OS / Node 版本 / 老己版本）、相关日志（注意**脱敏**，不要贴真实日记或密钥）。
- **功能建议**：说明想解决的问题、你的设想、有没有替代方案。

> 提 Issue 前请先搜索是否已有重复，避免分散讨论。

---

## 三、提 Pull Request

1. **Fork** 本仓库到你的账号；
2. 从 `main` 切出特性分支：`git checkout -b feat/xxx` 或 `fix/xxx`；
3. 提交改动，使用**约定式提交**风格（见下）；
4. `git push` 到你的 Fork，在 GitHub 发起 PR 到本仓库 `main`；
5. 在 PR 描述里关联对应 Issue（如 `Closes #12`）。

### 提交信息约定（Conventional Commits）
```
feat: 新增三我人格模板「疗愈者」
fix(widget): 修正竖图被裁切为横条的问题
docs: 补充本地模型接入说明
chore: 升级依赖版本
```

### 提交前自查清单
- [ ] `npm run build` 类型检查通过（无 `tsc` 报错）；
- [ ] 没有提交 `.env`、`data/`、`.tjkey` 或任何真实日记/密钥（它们已被 `.gitignore` 忽略）；
- [ ] 新增的依赖已在 `package.json` 中正确声明；
- [ ] 改动有必要的说明（尤其是行为变更）；
- [ ] 涉及 UI 的，附上自测结果或截图。

---

## 四、本地开发环境

详见 [`README.md`](./README.md) 的「快速开始」。简要流程：

```bash
npm install
cp .env.example .env      # 填入你自己的 LLM 接口信息
npm run dev               # 启动后端（127.0.0.1:3000）
npm run widget            # 启动桌宠窗口
```

---

## 五、素材与版权

- 软件**默认形象为原创角色「老己」**，不含任何真实人物肖像。
- 欢迎贡献同人形象 / 皮肤素材，请采用 **CC BY-SA 4.0** 协议发布，并在提交时注明作者与协议。
- **请勿提交包含真实人物肖像、或受版权保护的素材**（如未授权明星写真、他人摄影作品、网络随手扒来的图）。这类 PR 将被拒绝。
- ⚠️ 注意：**图片「公开可访问 / 网上能搜到」不等于「无版权、可自由使用」**。著作权自作品创作完成即产生，与是否公开发布无关；未经授权的网图（哪怕挂在公开网页上）同样受版权与肖像权保护，不能提交、也不建议用于本地个性化。只有在素材页明确标注 **CC0 / 公有领域 / CC BY(-SA)** 等开放协议，或为你本人原创时，才允许使用（CC BY 类需保留署名）。
- 你上传到**自己本机**的个性化素材由你自负版权与肖像权责任（见 README「个性化素材 · 用户自负声明」）。

---

## 六、许可证

本项目以 **AGPL-3.0-or-later** 开源。提交 PR 即表示你同意：你的贡献在**不另行声明**的情况下，以同等 AGPL-3.0-or-later 协议授权给本项目。

> 衍生作品（含以网络服务形式提供）同样须遵守 AGPL 的「提供对应源代码」义务。

---

再次感谢你的参与 🌱 有任何疑问，欢迎开 Issue 或在讨论区交流。
